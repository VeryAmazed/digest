/**
	Seg Tree needs a copy constructor and assignment operator override
	
	vector performs essentially same as array, except it does intially fill the memory with takes with 0s, or some other default value where arrays don't, so it is a bit more performant there
	
	
*/
#ifndef WIND_MIN_HPP
#define WIND_MIN_HPP
#include "digester.hpp"
namespace digest{
	
}
#endif
